FoamFile { format ascii; class dictionary; object momentumTransport; }
simulationType LES;
LES
{
    model           WALE;
    turbulence      on;
    delta           cubeRootVol;
    cubeRootVolCoeffs { deltaCoeff 1; }
}
